name: Submain Original Mafia CI

on:
  push:
    branches: ["submain"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Full project check
        run: npm run check

      - name: Original Mafia rule tests
        run: npm run mafia:original:test

      - name: Original Mafia chat 1,000-game Gate
        run: npm run mafia:original:sim

      - name: Chat-first local runtime and UI boundary
        run: npm run mafia:m3:ui

      - name: Build submain Web preview
        env:
          VITE_BASE_PATH: /taurin4/submain/
        run: npm run build

      - name: Upload Web preview artifact
        uses: actions/upload-artifact@v4
        with:
          name: submain-original-mafia-web-preview
          path: dist/
          if-no-files-found: error
          retention-days: 7
